# Local secrets
.env.simrunner.local

# Python
.venv/
__pycache__/
*.pyc
*.pyo

# SimC binary / downloads (large binaries, not source)
tools/

# Sim outputs (generated, not committed)
results/

# PyInstaller build artifacts and compiled EXEs
build/
build-patched/
build-patched-new/
dist/
dist-patched/
dist-patched-new/

# Temporary test/scratch folders
tmp-*/

# Input files with personal character data (committed selectively)
# input/ is tracked — add specific exclusions below if needed


# Local Claude agent settings
.claude/
